# BuildKit prefers <dockerfile>.dockerignore over .dockerignore, which lets the
# production build copy in the seeded database that the development build skips.
.DS_Store
.env
.git
.github
node_modules
storage/logs/*.log
tests
vendor
database/*.sqlite-shm
database/*.sqlite-wal
